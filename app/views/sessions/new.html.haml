%h1 Login Form

= link_to 'Back', root_path

= form_for(:user, url: login_path, method: :post) do |f|
  %div
    = f.label :email
    = f.text_field :email       
  %div
    = f.label :password
    = f.password_field :password
  = f.submit "Login"
